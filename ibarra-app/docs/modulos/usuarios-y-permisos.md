# Usuarios y permisos

## Resumen

Administra usuarios, roles y permisos granulares por módulo y acción.

## Código y relaciones

Se implementa en `src/app/components/user-management`, `role-management`, `role-permission-management` y `role-permissions-edit`. Peajes se integra mediante el módulo `peajes` y acción `read`.
